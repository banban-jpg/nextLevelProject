<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="login.mapper.LoginMapper">

    <insert id="insertUser" parameterType="UserAccountDTO">
        INSERT INTO USER_ACCOUNT (LOGIN_ID, PASSWORD_HASH, NAME, CREATED_AT)
        VALUES (#{loginId}, #{passwordHash}, #{name}, SYSDATE)
    </insert>

    <select id="countByLoginId" parameterType="string" resultType="int">
        SELECT COUNT(*) FROM USER_ACCOUNT WHERE LOGIN_ID = #{loginId}
    </select>

    <select id="findByLoginId" parameterType="string" resultType="UserAccountDTO">
        SELECT USER_ID, LOGIN_ID, PASSWORD_HASH, NAME, CREATED_AT
        FROM USER_ACCOUNT
        WHERE LOGIN_ID = #{loginId}
    </select>

    <select id="findByUserId" parameterType="long" resultType="UserAccountDTO">
        SELECT USER_ID, LOGIN_ID, PASSWORD_HASH, NAME, CREATED_AT
        FROM USER_ACCOUNT
        WHERE USER_ID = #{userId}
    </select>

</mapper>
